//ne koristiomo ovu semu 
/*const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const wishlistSchema = new Schema({
  saleId : {
    type: Schema.Types.ObjectId,
    ref: 'Sale',
    required: true
  },
  listOfCustomers : [{
    type: Schema.Types.ObjectId,
    ref: 'User',
    required: true
  }]
});

module.exports = mongoose.model('Wishlist', wishlistSchema);*/